services:
  client-gateway-db:
    container_name: client-gateway-postgres
    image: postgres:15.3
    environment:
      POSTGRES_DB: ${POSTGRES_DB} 
      POSTGRES_USER: ${POSTGRES_USER} 
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PGDATA: /data/postgres
    volumes:
      - postgres-data:/data/postgres
    ports:
      - "5432:5432"
    networks:
      - gateway-network
    restart: unless-stopped

  client-gateway-service:
    container_name: client-gateway
    build: .
    ports:
      - "8081:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://client-gateway-db:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER} 
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      SPRING_PROFILES_ACTIVE: dev
      CLIENT_MANAGEMENT_API_BASE_URL: http://client-management-api:8080
    networks:
      - gateway-network
    depends_on:
      - client-gateway-db
    restart: unless-stopped
    
networks:
  gateway-network:
    driver: bridge

volumes:
  postgres-data:
